def http_request(settings, headers, body) {
    var input = to_json([
        "settings": settings,
        "headers": headers,
        "body": body
    ])
    //print(input)
    var output = _http_request(input)
    //print(output)
    return from_json(output)
}
